name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Lint · Test · Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Build all packages
        run: npm run build

      - name: Validate example courses
        run: npm run validate:examples

  commitlint:
    name: Commit messages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Lint PR commits
        if: github.event_name == 'pull_request'
        run: npx commitlint --from "${{ github.event.pull_request.base.sha }}" --to "${{ github.event.pull_request.head.sha }}" --verbose

  # ──────────────────────────────────────────────────────────────────────────
  # SCORM Cloud conformance (stubbed — enable once the packager lands).
  # Posts built SCORM/cmi5 packages to SCORM Cloud's test harness for real-LMS
  # verification. Secrets (SCORM_CLOUD_APP_ID / SCORM_CLOUD_SECRET_KEY) live in
  # CI only — never replicate SCORM Cloud locally (see CLAUDE.md).
  #
  # scorm-cloud:
  #   name: SCORM Cloud conformance
  #   runs-on: ubuntu-latest
  #   needs: verify
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - run: npm ci
  #     - run: npm run build
  #     - name: Package examples for SCORM Cloud
  #       run: npx oelt package examples/minimal --target scorm2004
  #     - name: Upload to SCORM Cloud and run conformance checks
  #       env:
  #         SCORM_CLOUD_APP_ID: ${{ secrets.SCORM_CLOUD_APP_ID }}
  #         SCORM_CLOUD_SECRET_KEY: ${{ secrets.SCORM_CLOUD_SECRET_KEY }}
  #       run: node scripts/scorm-cloud-conformance.mjs
