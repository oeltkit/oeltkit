#!/usr/bin/env node
/**
 * @oeltkit/cli — entry point.
 *
 * Scaffold only. The `oelt` commands (package / preview / validate / new) are
 * implemented in later tasks per `specs/`. This stub exists so the package
 * builds and exposes its bin.
 */

/** Package version placeholder; real version is injected at publish time. */
export const VERSION = "0.0.0";
