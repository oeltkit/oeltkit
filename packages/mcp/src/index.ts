#!/usr/bin/env node
/**
 * @oeltkit/mcp — entry point.
 *
 * Scaffold only. The MCP server (course scaffolding, page CRUD, validate,
 * preview, package tools per `docs/PLAN.md` §4.6) is implemented in a later
 * task. This stub exists so the package builds and exposes its bin.
 */

/** Package version placeholder; real version is injected at publish time. */
export const VERSION = "0.0.0";
