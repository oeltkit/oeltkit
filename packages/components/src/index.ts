/**
 * @oeltkit/components — entry point.
 *
 * Scaffold only. Custom elements (<oelt-*>) are implemented in a later task,
 * each against its contract in `specs/components/<name>.md`. This stub exists
 * so the dual-output build (ESM + IIFE) produces artifacts.
 */

/** Package version placeholder; real version is injected at publish time. */
export const VERSION = "0.0.0";
