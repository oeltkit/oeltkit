/**
 * @oeltkit/runtime — entry point.
 *
 * Scaffold only. Runtime services (tracking, state, navigation, a11y) are
 * implemented in a later task per `specs/tracking-semantics.md`. This stub
 * exists so the dual-output build (ESM + IIFE) produces artifacts.
 */

/** Manifest schema major.minor this runtime build targets. */
export const MANIFEST_VERSION = "0.1";

/** Package version placeholder; real version is injected at publish time. */
export const VERSION = "0.0.0";
